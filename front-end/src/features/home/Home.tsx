import React from 'react'
import {
    Box
} from '@mui/material';

const Home = () => {

    return (
        <Box >
            {/* <SearchBar search={search} setSearch={setSearch} onSearch={onSearch} /> */}
        </Box>
    )
}

export default Home;